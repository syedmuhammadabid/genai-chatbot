{
  "name": "genai-chatbot",
  "version": "1.0.0",
  "scripts": {
    "install": "npm run install:all",
    "install:all": "npm install --prefix backend && npm install --prefix frontend && echo All dependencies installed",
    "start": "pm2 start ecosystem.config.js",
    "stop": "pm2 stop all",
    "restart": "pm2 restart all"
  },
  "dependencies": {
    "pm2": "^5.3.0"
  }
}
